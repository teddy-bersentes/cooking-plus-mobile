import React from 'react';
import { AppStackScreenProps } from './AppNavigator';
import { View, Screen, Text } from '~/components';

type Props = AppStackScreenProps<'ImportRecipe'>;

export function ImportRecipeScreen({ navigation }: Props) {
	return (
		<Screen>
		</Screen>
	)
}